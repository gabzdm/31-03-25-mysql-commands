create database `gabriela`;
# a linha acima serve para criar um banco de dados com o nome `gabriela`
#o uso da crase se torna necessário para indicar um `string` no sistema 
# `strings` do sistema: `database` , `table` e `campo`
 